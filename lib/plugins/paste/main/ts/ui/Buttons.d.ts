import { Editor } from 'tinymce/core/api/Editor';
import { Clipboard } from '../api/Clipboard';
declare const _default: {
    register: (editor: Editor, clipboard: Clipboard) => void;
};
export default _default;
